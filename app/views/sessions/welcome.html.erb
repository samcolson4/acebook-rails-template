<% if logged_in? %>
  <h1>Welcome <%= current_user.name %></h1>
  <% else %>
  <h1> Welcome </h1>
  <%= button_to "Sign Up", '/users/new', method: :get, :class => "button"%>
 <%= button_to "Log In", '/login', method: :get, :class => "button" %>
<% end %>
