require 'rails_helper'

RSpec.describe LikesController, type: :controller do

  # describe "POST /" do
  #   it "add a like to a post" do
  #     User.create(name: "Bob", email: "bob@bob.com", password: "1234567")
  #     user = User.find_by(name: "Bob")
  #     allow(User).to receive(:find_by).and_return(user)
  #     Post.create(message: "Hello, world!", user_id: user.id )
  #     post = Post.find_by(message: "Hello, world!")
  #     allow(Post).to receive(:find).and_return(post.id)
  #     post post_likes_path, params: { post_id: post.id, user_id: user.id }
  #   end
  # end

end
